import { ProjectRepository } from '../../domain/repositories/project.repository'

export class ProjectRepositoryImpl implements ProjectRepository {
    // Aquí irá la implementación del repositorio para Project
}